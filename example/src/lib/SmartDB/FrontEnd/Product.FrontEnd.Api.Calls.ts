import { BaseFrontEndApiCalls } from 'smart-db';
import { ProductEntity } from '../Entities/Product.Entity';

export class ProductApi extends BaseFrontEndApiCalls {
    protected static _Entity = ProductEntity;

    // #region api

    // #endregion api
}
