import { BaseFrontEndApiCalls } from 'smart-db';
import { ProductOptEntity } from '../Entities';

export class ProductOptApi extends BaseFrontEndApiCalls {
    protected static _Entity = ProductOptEntity;

    // #region api

    // #endregion api
}
