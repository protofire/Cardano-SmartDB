import { BaseFrontEndApiCalls } from 'smart-db';
import { UserOptEntity } from '../Entities';

export class UserOptApi extends BaseFrontEndApiCalls {
    protected static _Entity = UserOptEntity;

    // #region api

    // #endregion api
}
