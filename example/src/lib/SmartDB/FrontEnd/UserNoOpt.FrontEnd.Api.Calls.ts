import { BaseFrontEndApiCalls } from 'smart-db';
import { UserNoOptEntity } from '../Entities';

export class UserNoOptApi extends BaseFrontEndApiCalls {
    protected static _Entity = UserNoOptEntity;

    // #region api

    // #endregion api
}
